card mutt {
    alternatives $(HOSTNAME) $(HOST) shared
    into ~/.mutt
    outof mutt

    sig      -> sig
    mailcap  -> mailcap
    keys     -> keys
    colors   -> colors
    alias    -> alias
    muttrc   -> muttrc
    specific -> specific
}
