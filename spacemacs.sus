card spacemacs {
    alternatives $(HOSTNAME) $(HOST) shared
    into ~
    outof spacemacs

    spacemacs -> .spacemacs

    {
        into .emacs.d/private
    
        private-org -> private-org
    }
}
