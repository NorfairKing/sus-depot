card xmonad {
  alternatives $(HOST) shared

  into ~
  outof xmonad
  
  .xmobarrc

  {
    into .xmonad

    xmonad.hs
    makefile

    {
      into lib

      Actions.hs
      Constants.hs
      Internet.hs
      Keys.hs
      Layout.hs
      Workspaces.hs
      Modifier.hs
      Solarized.hs
    }
  }
}
