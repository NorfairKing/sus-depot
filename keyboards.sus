card keyboards {
  alternatives $(HOST) shared
  alternatives $(USER) shared
  into ~/.keyboards

  {
    outof keyboards

    numpad.qwerty
    kinesis.dvorak
    laptop.dvorak
    $(KEYBOARD) -> current
  }
}
