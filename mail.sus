card mail {
  alternatives $(HOST) shared
  alternatives $(USER) shared
  into ~
  outof mail

  {
    into .mutt
    outof mutt

    sig
    mailcap
    keys
    gpg
    colors
    alias
    muttrc
  }
}
